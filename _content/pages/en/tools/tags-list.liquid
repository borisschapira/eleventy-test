---
permalink: /tags/
layout: default.liquid
---
<h1>Tags</h1>

{% for tag in collections.tagList %}
  {% capture tagUrl %}/tags/{{ tag | slug }}/{% endcapture %}
  <a href="{{ tagUrl | url }}" class="tag">{{ tag }}</a>
{% endfor %}
